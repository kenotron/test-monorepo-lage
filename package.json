{
  "name": "test-monorepo-lage",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "scripts"
    ]
  },
  "scripts": {
    "clean": "git clean -fdx -e node_modules",
    "build": "lage build",
    "typecheck": "lage typecheck",
    "test": "lage test",
    "lint": "lage lint",
    "lage": "lage"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "lage": "0.34.1-fallback.2",
    "beachball": "^2.1.0"
  }
}
